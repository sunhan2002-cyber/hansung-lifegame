# 4주차 팀원 개발 숙제

## 1. 현재 상태 확인

현재 폴더에는 Android Studio 프로젝트 코드가 아직 없다. 이번 주 숙제는 문서만 작성하는 것이 아니라, 팀원이 바로 커밋할 수 있는 Android 앱 뼈대와 샘플 데이터를 만드는 것이다.

기준 문서는 다음 파일이다.

| 파일 | 확인할 내용 |
| --- | --- |
| `텍스트_기반_인생게임_프로젝트_초안.md` | 게임 흐름, 7개 지표, 돌발 이벤트, 최종 콘텐츠 목표 |
| `명세서.md` | 기능 요구사항, 데이터 명세, 검증 기준 |
| `팀원 역할표 초안.md` | 역할명과 담당 범위 |

이번 주 공통 전제는 다음과 같다.

- Android 패키지명은 임시로 `com.example.lifegame`을 사용한다.
- Android 프로젝트가 생성되면 모든 역할은 이 패키지명을 기준으로 파일을 만든다.
- 패키지명을 바꾸면 모든 파일 경로와 import를 같은 이름으로 함께 바꾼다.
- UI는 Jetpack Compose 기준으로 작성한다.
- 데이터는 우선 `app/src/main/assets/`의 JSON 파일로 읽는 구조를 목표로 한다.

## 2. 문제점과 보강 방향

| 문제점 | 보강 방향 |
| --- | --- |
| Android 프로젝트 코드가 없음 | 프론트엔드 담당이 실행 가능한 Android 프로젝트와 화면 파일을 만든다. |
| 화면 흐름이 문서에만 있음 | 시작 → 캐릭터 설정 → 게임 진행 → 선택 결과 → 결말 화면 이동을 실제 앱에 만든다. |
| 게임 규칙이 코드가 아님 | 백엔드·게임 로직 담당이 지표 계산, 선택 반영, 결말 판정 함수를 만든다. |
| 사건 데이터가 앱에 연결되지 않음 | 시나리오 담당과 DB 담당이 JSON 샘플과 로더 구조를 만든다. |
| 테스트가 추상적임 | 팀장이 일반·위험·애매한 테스트를 실행 기준으로 정리하고, Gradle 테스트 통과 여부를 확인한다. |

## 3. 역할별 커밋 가능한 숙제

| 역할 | 커밋 단위 | 생성·수정 파일 |
| --- | --- | --- |
| 프론트엔드 개발 담당 | 앱 실행과 화면 이동 뼈대 구현 | `settings.gradle.kts`, `build.gradle.kts`, `app/build.gradle.kts`, `app/src/main/AndroidManifest.xml`, `app/src/main/java/com/example/lifegame/MainActivity.kt`, `app/src/main/java/com/example/lifegame/ui/screens/*.kt` |
| UI/UX 디자인 담당 | 공통 UI 컴포넌트와 화면 스타일 구현 | `app/src/main/java/com/example/lifegame/ui/theme/*.kt`, `app/src/main/java/com/example/lifegame/ui/components/*.kt` |
| 백엔드·게임 로직 담당 | 지표 계산, 선택 반영, 다음 사건·결말 판정 로직 구현 | `app/src/main/java/com/example/lifegame/domain/model/GameModels.kt`, `app/src/main/java/com/example/lifegame/domain/engine/GameEngine.kt`, `app/src/test/java/com/example/lifegame/domain/engine/GameEngineTest.kt` |
| 시나리오·콘텐츠 작성 담당 | 앱에 넣을 사건 JSON 작성 | `app/src/main/assets/events.sample.json` |
| DB·콘텐츠 데이터 담당 | JSON 구조와 로더, 결말·이미지 데이터 작성 | `app/src/main/assets/endings.sample.json`, `app/src/main/assets/image_ids.sample.json`, `app/src/main/java/com/example/lifegame/data/ContentRepository.kt`, `app/src/main/java/com/example/lifegame/data/ProgressStore.kt` |
| 팀장: 총괄·기획, QA·문서화 | 테스트 기준, 변경 전후 비교, 8주 범위 관리 | `docs/qa/test_cases.md`, `docs/qa/before_after_check.md`, `docs/qa/8week_scope.md` |

## 4. 역할별 상세 작업

### 프론트엔드 개발 담당

목표는 Android Studio에서 앱이 실행되고, 사용자가 화면 5개를 순서대로 이동할 수 있게 만드는 것이다.

생성·수정 파일:

| 파일 | 작업 내용 |
| --- | --- |
| `settings.gradle.kts` | 프로젝트 이름을 `LifeGame`으로 설정하고 `:app` 모듈을 포함한다. |
| `build.gradle.kts` | Android Gradle Plugin과 Kotlin 플러그인 버전을 선언한다. |
| `app/build.gradle.kts` | `com.android.application`, Kotlin Android, Compose 사용 설정을 넣는다. |
| `app/src/main/AndroidManifest.xml` | `MainActivity`를 시작 Activity로 등록한다. |
| `MainActivity.kt` | `setContent { LifeGameApp() }`를 호출한다. |
| `ui/screens/StartScreen.kt` | 앱 제목, `새 게임`, `이어하기` 버튼을 만든다. |
| `ui/screens/CharacterSelectScreen.kt` | 기본 아기 이미지 3개 선택 영역, `게임 시작` 버튼을 만든다. |
| `ui/screens/GameScreen.kt` | 성장 단계, 상황 이미지 영역, 상황 텍스트, A/B 선택 버튼, 7개 지표 표시 영역을 만든다. |
| `ui/screens/ChoiceResultScreen.kt` | 선택 결과 문장, 변화한 지표, `다음 사건` 버튼을 만든다. |
| `ui/screens/EndingScreen.kt` | 결말 제목, 결말 설명, 최종 지표, 주요 선택 기록, `다시 시작` 버튼을 만든다. |

필수 동작:

1. 앱 실행 시 `StartScreen`이 보여야 한다.
2. `새 게임` 버튼을 누르면 `CharacterSelectScreen`으로 이동해야 한다.
3. `게임 시작` 버튼을 누르면 `GameScreen`으로 이동해야 한다.
4. `GameScreen`의 A 또는 B 버튼을 누르면 `ChoiceResultScreen`으로 이동해야 한다.
5. `ChoiceResultScreen`의 `다음 사건` 버튼을 누르면 다시 `GameScreen`으로 이동해야 한다.
6. 임시 버튼이나 조건으로 `EndingScreen` 이동을 확인할 수 있어야 한다.

테스트 기준:

| 구분 | 확인할 내용 |
| --- | --- |
| 일반 | 앱 실행 후 시작 화면이 보인다. |
| 일반 | 새 게임 → 캐릭터 설정 → 게임 진행 화면으로 이동한다. |
| 위험 | A/B 버튼을 빠르게 눌러도 앱이 멈추지 않는다. |
| 애매 | 이어하기 데이터가 아직 없을 때 이어하기 버튼을 비활성화하거나 안내 문구를 보여준다. |

완료 기준:

- Android Studio 또는 Gradle에서 앱이 빌드된다.
- 화면 5개 파일이 존재한다.
- 화면 이동이 실제 앱에서 확인된다.
- 아직 실제 이미지가 없어도 이미지 자리 표시 영역은 화면에 존재한다.

### UI/UX 디자인 담당

목표는 프론트엔드 화면을 사용자가 읽고 누르기 쉽게 만드는 공통 컴포넌트를 준비하는 것이다.

생성·수정 파일:

| 파일 | 작업 내용 |
| --- | --- |
| `ui/theme/Color.kt` | 기본 배경색, 일반 사건 색, 돌발 이벤트 강조색, 버튼 색을 정의한다. |
| `ui/theme/Type.kt` | 제목, 상황 설명, 선택 버튼, 지표 텍스트 크기를 정의한다. |
| `ui/theme/Theme.kt` | 앱 전체 테마를 만든다. |
| `ui/components/StatBar.kt` | 7개 지표 중 하나를 이름, 수치, 막대로 보여주는 컴포넌트를 만든다. |
| `ui/components/ChoiceButton.kt` | A/B 선택 버튼 공통 컴포넌트를 만든다. |
| `ui/components/EventImageBox.kt` | 상황 이미지 또는 임시 이미지를 보여주는 영역을 만든다. |
| `ui/components/EventCard.kt` | 상황 텍스트와 이미지를 함께 보여주는 카드형 영역을 만든다. |

필수 UI 기준:

1. `GameScreen`에서 상황 이미지 영역은 화면 너비의 대부분을 차지하되, 선택 버튼을 가리지 않는다.
2. 상황 텍스트가 길면 스크롤되거나 3줄 이상 표시될 수 있어야 한다.
3. A/B 선택 버튼은 한 손 조작을 고려해 충분히 크게 만든다.
4. 7개 지표는 한 화면에서 지나치게 복잡해 보이지 않도록 2열 또는 세로 목록으로 정리한다.
5. 돌발 이벤트는 일반 사건과 배경색 또는 라벨로 구분한다.

테스트 기준:

| 구분 | 확인할 내용 |
| --- | --- |
| 일반 | 선택 버튼 글자가 두 줄이 되어도 버튼 밖으로 나가지 않는다. |
| 일반 | 7개 지표가 화면에서 겹치지 않는다. |
| 위험 | 긴 상황 문장 200자 이상을 넣어도 선택 버튼이 화면 밖으로 밀리지 않는다. |
| 애매 | 돌발 이벤트 강조가 너무 과해서 일반 화면 흐름을 방해하지 않는다. |

완료 기준:

- 공통 컴포넌트 4개 이상이 존재한다.
- `GameScreen`에서 `StatBar`, `ChoiceButton`, `EventImageBox`를 사용한다.
- 작은 화면 기준으로 텍스트와 버튼 겹침이 없어야 한다.

### 백엔드·게임 로직 담당

목표는 선택지를 눌렀을 때 7개 지표가 계산되고, 다음 사건과 결말을 판정할 수 있는 순수 Kotlin 로직을 만드는 것이다.

생성·수정 파일:

| 파일 | 작업 내용 |
| --- | --- |
| `domain/model/GameModels.kt` | `Stats`, `GameEvent`, `Choice`, `Ending`, `GameProgress`, `ChoiceResult` 데이터 클래스를 만든다. |
| `domain/engine/GameEngine.kt` | 지표 계산, 선택 반영, 다음 사건 선택, 결말 판정 함수를 만든다. |
| `domain/engine/GameEngineTest.kt` | 일반·위험·애매한 테스트를 작성한다. |

구현할 메서드:

| 메서드 | 동작 |
| --- | --- |
| `clampStat(value: Int): Int` | 지표 값을 0~100 범위로 제한한다. |
| `applyChoice(progress: GameProgress, event: GameEvent, choice: Choice): ChoiceResult` | 선택 결과를 지표와 경험에 한 번 반영한다. |
| `canShowEvent(progress: GameProgress, event: GameEvent): Boolean` | 사건 등장 조건을 만족하는지 확인한다. |
| `pickNextEvent(progress: GameProgress, events: List<GameEvent>): GameEvent?` | 조건에 맞는 다음 사건을 고른다. |
| `resolveEnding(progress: GameProgress, endings: List<Ending>): Ending` | 조건과 우선순위에 따라 결말을 고른다. |
| `shouldTriggerSpecialEvent(progress: GameProgress, chancePercent: Int): Boolean` | 돌발 이벤트 등장 여부를 판정한다. |

테스트 기준:

| 구분 | 테스트 |
| --- | --- |
| 일반 | 지력 50에서 지력 +6 선택 시 지력 56이 된다. |
| 일반 | 사회성 조건을 만족하면 사회성 조건 사건이 등장한다. |
| 위험 | 건강 98에서 +5를 적용해도 100을 넘지 않는다. |
| 위험 | 같은 선택을 두 번 눌러도 같은 사건 발생 ID에는 한 번만 반영된다. |
| 애매 | 여러 결말 조건을 동시에 만족하면 priority가 높은 결말이 선택된다. |
| 애매 | 조건에 맞는 사건이 없으면 null 또는 공통 사건 처리 기준을 따른다. |

완료 기준:

- 위 메서드가 모두 존재한다.
- 단위 테스트 6개 이상이 존재한다.
- `./gradlew test`에서 게임 로직 테스트가 통과한다.

### 시나리오·콘텐츠 작성 담당

목표는 앱에서 바로 읽을 수 있는 사건 JSON을 만드는 것이다. 이번 주 기준은 80개 사건이다.

생성·수정 파일:

| 파일 | 작업 내용 |
| --- | --- |
| `app/src/main/assets/events.sample.json` | 사건 80개 이상 작성 |

작성 개수:

| 성장 단계 | 사건 수 |
| --- | ---: |
| 유아기 | 15개 이상 |
| 아동기 | 20개 이상 |
| 청소년기 | 20개 이상 |
| 성인기 | 20개 이상 |
| 특수 돌발 이벤트 | 5개 이상 |

각 사건 JSON 필수 항목:

```json
{
  "eventId": "child_001",
  "stage": "아동기",
  "type": "normal",
  "title": "방과 후 선택",
  "text": "학교가 끝났다. 오늘 남은 시간을 어떻게 보낼까?",
  "imageId": "img_child_school_001",
  "conditions": {
    "requiredStats": {},
    "requiredFlags": [],
    "blockedFlags": []
  },
  "choices": [
    {
      "choiceId": "A",
      "label": "도서관에서 책을 읽는다.",
      "statDelta": {
        "지력": 6,
        "행복": 1
      },
      "resultText": "새로운 지식을 얻고 독서에 관심이 생긴다.",
      "addFlags": ["reading_interest"]
    },
    {
      "choiceId": "B",
      "label": "친구들과 운동장에서 논다.",
      "statDelta": {
        "운동능력": 4,
        "사회성": 4,
        "행복": 2
      },
      "resultText": "친구들과 가까워지고 몸을 움직이며 기분이 좋아진다.",
      "addFlags": ["playground_friendship"]
    }
  ]
}
```

작성 규칙:

- 모든 사건에는 A/B 선택지 2개를 넣는다.
- 모든 선택지에는 결과 문장을 넣는다.
- `statDelta`는 7개 지표 중 변화가 있는 지표만 적는다.
- 특수 돌발 이벤트는 `type`을 `special`로 적는다.
- 돌발 이벤트 5개는 개복치 게임처럼 과장되고 어이없는 상황으로 작성한다.
- 최소 40개 사건은 완성 문장으로 작성한다.
- 나머지 40개 사건은 제목, 상황 문장 초안, A/B 선택지, 의도한 지표 변화까지 작성한다.

테스트 기준:

| 구분 | 확인할 내용 |
| --- | --- |
| 일반 | 모든 사건에 A/B 선택지가 있다. |
| 일반 | 모든 선택지에 `resultText`가 있다. |
| 위험 | `eventId`가 중복되지 않는다. |
| 위험 | 7개 지표 이름 외의 다른 지표명이 들어가지 않는다. |
| 애매 | 너무 비슷한 사건은 제목이나 상황을 바꿔 구분한다. |

완료 기준:

- JSON 형식이 깨지지 않는다.
- 사건 80개 이상이 있다.
- 선택지 160개 이상이 있다.
- 돌발 이벤트 5개 이상이 있다.

### DB·콘텐츠 데이터 담당

목표는 시나리오 데이터를 앱이 읽고 저장할 수 있는 데이터 구조를 만드는 것이다.

생성·수정 파일:

| 파일 | 작업 내용 |
| --- | --- |
| `app/src/main/assets/endings.sample.json` | 결말 20개 이상 작성 |
| `app/src/main/assets/image_ids.sample.json` | 이미지 ID 80개 이상 작성 |
| `data/ContentRepository.kt` | assets JSON을 읽는 Repository 뼈대 작성 |
| `data/ProgressStore.kt` | 현재 사건 ID, 지표, 경험, 선택 이력을 저장하는 구조 작성 |
| `docs/data_schema.md` | 사건, 선택지, 결말, 이미지, 저장 상태 JSON 항목 설명 |

`endings.sample.json` 필수 항목:

```json
{
  "endingId": "ending_balanced_life",
  "title": "균형을 이룬 삶",
  "priority": 50,
  "conditions": {
    "minStats": {
      "건강": 45,
      "운동능력": 45,
      "지력": 45,
      "사회성": 45,
      "경제력": 45,
      "행복": 60
    },
    "requiredFlags": []
  },
  "summary": "여러 선택을 균형 있게 쌓아 안정적인 삶을 만들었다."
}
```

`image_ids.sample.json` 필수 항목:

```json
{
  "imageId": "img_child_school_001",
  "usage": "event",
  "description": "아동기 학교 또는 교실 상황 이미지",
  "fileName": "img_child_school_001.png"
}
```

구현할 클래스·메서드:

| 파일 | 메서드 | 동작 |
| --- | --- | --- |
| `ContentRepository.kt` | `loadEvents(): List<GameEvent>` | assets의 사건 JSON을 읽는다. |
| `ContentRepository.kt` | `loadEndings(): List<Ending>` | assets의 결말 JSON을 읽는다. |
| `ProgressStore.kt` | `saveProgress(progress: GameProgress)` | 현재 진행 상태를 저장한다. |
| `ProgressStore.kt` | `loadProgress(): GameProgress?` | 저장된 진행 상태를 불러온다. |
| `ProgressStore.kt` | `clearProgress()` | 새 게임 시작 시 저장 상태를 초기화한다. |

테스트 기준:

| 구분 | 확인할 내용 |
| --- | --- |
| 일반 | 사건 JSON을 읽었을 때 사건 목록이 비어 있지 않다. |
| 일반 | 결말 JSON을 읽었을 때 결말 20개 이상이 있다. |
| 위험 | JSON 형식 오류가 있을 때 앱이 강제 종료되지 않는다. |
| 위험 | 저장된 현재 사건 ID가 없는 사건을 가리키면 새 게임 안내로 처리한다. |
| 애매 | 이미지 파일이 아직 없어도 `imageId` 설명으로 대체 표시할 수 있다. |

완료 기준:

- 결말 20개 이상이 있다.
- 이미지 ID 80개 이상이 있다.
- Repository와 ProgressStore 파일이 존재한다.
- JSON 항목 설명 문서가 존재한다.

### 팀장: 총괄·기획, QA·문서화

목표는 각 역할의 작업이 합쳐졌을 때 앱이 실제로 시연 가능한지 확인하는 것이다.

생성·수정 파일:

| 파일 | 작업 내용 |
| --- | --- |
| `docs/qa/test_cases.md` | 일반 10개, 위험 10개, 애매한 사례 10개 이상 작성 |
| `docs/qa/before_after_check.md` | 변경 전후 비교표 작성 |
| `docs/qa/8week_scope.md` | 8주 안 완성 범위와 주차별 목표 작성 |
| `docs/qa/review_checklist.md` | PR 또는 커밋 전 확인 체크리스트 작성 |

`test_cases.md` 필수 형식:

| 항목 | 설명 |
| --- | --- |
| 테스트 ID | TC-W4-001 형식 |
| 구분 | 일반 / 위험 / 애매 |
| 확인 대상 파일 | 어떤 파일을 확인하는지 |
| 조건·입력 | 어떤 상황을 넣는지 |
| 기대 결과 | 통과 기준 |

`8week_scope.md` 필수 기준:

| 항목 | 4주차 목표 | 8주 안 목표 | 최종 확장 목표 |
| --- | --- | --- | --- |
| 사건 | 80개 이상 | 200개 이상 | 300~500개 이상 |
| 선택지 | 160개 이상 | 400개 이상 | 1,000개 이상 |
| 결말 | 20개 이상 | 30개 이상 | 30개 이상 |
| 돌발 이벤트 | 5개 이상 | 30개 이상 | 50개 이상 |

확인할 명령:

```powershell
./gradlew test
./gradlew assembleDebug
```

테스트 기준:

| 구분 | 확인할 내용 |
| --- | --- |
| 일반 | 앱이 빌드되고 시작 화면이 보인다. |
| 일반 | 새 게임에서 게임 진행 화면까지 이동한다. |
| 위험 | A/B 버튼 연속 클릭으로 지표가 중복 반영되지 않는다. |
| 위험 | JSON 오류가 있을 때 앱이 강제 종료되지 않는다. |
| 애매 | 사건 수가 많아져도 팀이 검토할 수 있는 ID 규칙이 유지된다. |

완료 기준:

- 테스트 케이스 30개 이상이 있다.
- 변경 전후 비교 기준이 있다.
- 8주 범위표가 있다.
- 빌드와 테스트 결과를 기록한다.

## 5. 이번 주 커밋 기준

각 역할은 자기 작업을 한 번에 큰 덩어리로 올리지 말고, 아래 단위로 커밋할 수 있게 만든다.

| 역할 | 권장 커밋 메시지 |
| --- | --- |
| 프론트엔드 개발 담당 | `feat: add basic compose screen flow` |
| UI/UX 디자인 담당 | `feat: add common game UI components` |
| 백엔드·게임 로직 담당 | `feat: add game engine stat and ending logic` |
| 시나리오·콘텐츠 작성 담당 | `content: add initial event sample data` |
| DB·콘텐츠 데이터 담당 | `feat: add content repository and sample data schema` |
| 팀장: 총괄·기획, QA·문서화 | `docs: add week 4 QA and scope checklist` |

커밋 전 확인:

- 새 파일 경로가 역할별 숙제와 일치하는가
- Android 프로젝트가 빌드되는가
- JSON 형식이 깨지지 않았는가
- 사건 ID, 결말 ID, 이미지 ID가 중복되지 않는가
- 일반·위험·애매한 테스트 기준이 작성되었는가

