# 3주차 팀원 숙제

## 1. 현재 상태 확인

현재 프로젝트는 Android Studio 앱 구현 전 단계이며, 코드 파일은 아직 없다. 이번 주 숙제는 다음 구현 단계로 넘어가기 위한 문서와 데이터 초안을 만드는 작업으로 진행한다.

확인 기준 파일은 다음과 같다.

| 파일 | 확인할 내용 |
| --- | --- |
| `텍스트_기반_인생게임_프로젝트_초안.md` | 게임 목표, 진행 흐름, 7개 지표, 특수 돌발 이벤트, 선택지 1,000개 이상과 결말 30개 이상 목표 |
| `명세서.md` | 기능 요구사항, 게임 규칙, 데이터 명세, 검증 사례, SDD 개발 절차 |
| `팀원 역할표 초안.md` | 팀장 고정 역할과 팀원 역할 후보 |

## 2. 문제점과 보강 방향

현재 초안은 게임의 큰 방향은 잡혀 있지만, 구현에 바로 들어가기에는 다음 항목이 더 필요하다.

| 문제점 | 보강 방향 |
| --- | --- |
| 사건, 선택지, 결말 데이터의 실제 예시가 부족함 | 이번 주부터 사건 수백 개 확장을 전제로 사건·선택지·결말 초안을 만든다. |
| 선택지 1,000개 이상 목표에 비해 데이터 구조가 아직 구체적이지 않음 | 사건 ID, 선택지 ID, 이미지 ID, 조건, 지표 변화량 형식을 먼저 정하고, 최소 200개 사건까지 늘릴 수 있는 구조로 작성한다. |
| UI 화면 흐름이 글로만 있고 화면 단위가 부족함 | 시작, 캐릭터 설정, 게임 진행, 결과, 결말 화면의 표시 요소를 정리한다. |
| 돌발 이벤트가 재미 요소로 추가됐지만 확률과 결과 기준이 아직 모호함 | 일반 사건과 돌발 이벤트의 등장 조건, 위험도, 결과 판정 기준을 분리한다. |
| 테스트 기준은 있으나 팀원이 바로 확인할 테스트 묶음이 부족함 | 일반, 위험, 애매한 사례로 나눠 테스트 케이스를 작성한다. |
| 시나리오 담당 혼자 콘텐츠를 모두 쓰면 부담이 크고 병목이 생김 | 시나리오 담당은 양식과 핵심 사건을 만들고, 나머지 역할도 자기 영역과 관련된 사건 아이디어를 추가한다. |

## 3. 이번 주 역할별 숙제

숙제는 `팀원 역할표 초안.md`에 정리한 역할명을 기준으로 부여한다. 역할은 아직 팀원에게 배정하지 않는다. 팀 회의에서 각자 맡을 역할을 정한 뒤, 자신이 맡은 역할에 해당하는 파일을 작성한다.

| 역할 | 이번 주 구체 숙제 | 작성 파일 |
| --- | --- | --- |
| 프론트엔드 개발 담당 | 화면 5개를 기준으로 각 화면의 표시 요소, 버튼, 입력값, 다음 이동 화면을 표로 작성한다. 화면 5개는 시작, 캐릭터 설정, 게임 진행, 선택 결과, 인생 결과다. | `docs/ui_flow.md` |
| UI/UX 디자인 담당 | 화면 5개에 대해 이미지 위치, 텍스트 영역, 버튼 배치, 작은 화면 대응, 돌발 이벤트 강조 방식을 작성한다. 각 화면마다 최소 3개 이상의 디자인 기준을 적는다. | `docs/ui_ux_guideline.md` |
| 백엔드·게임 로직 담당 | 7개 지표 계산 규칙, 일반 선택 가중치 범위, 돌발 이벤트 확률, 사망 위기와 실제 사망 기준, 결말 우선순위 규칙을 항목별로 작성한다. 예시 계산 5개와 분기 조건 예시 20개를 포함한다. | `docs/game_rules.md` |
| 시나리오·콘텐츠 작성 담당 | 사건 80개 이상을 JSON으로 작성한다. 유아기 15개, 아동기 20개, 청소년기 20개, 성인기 20개, 특수 돌발 이벤트 5개 이상을 포함하고, 각 사건에는 A·B 선택지와 결과 문장을 반드시 넣는다. | `data/events.sample.json` |
| DB·콘텐츠 데이터 담당 | 사건·선택지·결말·이미지·저장 상태에 필요한 JSON 항목의 이름, 자료형, 필수 여부, 예시를 표로 작성한다. 결말 샘플 20개 이상과 이미지 ID 목록 80개 이상도 작성한다. | `data/content_schema.md`, `data/endings.sample.json`, `data/image_ids.sample.json` |
| 팀장: 총괄·기획, QA·문서화 | 각 역할 파일을 검토할 기준을 만든다. 일반 사례 10개, 위험 사례 10개, 애매한 사례 10개 이상을 작성하고, 변경 전후 비교표와 8주 완성 범위 판단표를 만든다. | `docs/test_cases.md`, `docs/before_after_check.md`, `docs/8week_scope.md` |

시나리오·콘텐츠 작성 담당은 이야기와 문장 자체를 만든다. DB·콘텐츠 데이터 담당은 그 이야기를 앱에서 읽을 수 있도록 ID, 조건, 이미지 연결, 저장 구조로 정리한다.

이번 주 콘텐츠 목표는 사건 80개 이상, 선택지 160개 이상, 결말 20개 이상이다. 이 수치는 최종 목표인 선택지 1,000개 이상과 결말 30개 이상으로 확장하기 위한 1차 콘텐츠 묶음이다. 이후 매주 사건을 추가해 8주 안에 사건 200개 이상, 선택지 400개 이상, 결말 30개 이상을 목표로 한다.

## 4. 파일별 작성 지시

### `docs/ui_flow.md`

프론트엔드 개발 담당이 작성한다. 화면 5개를 모두 포함하고, 각 화면마다 아래 항목을 채운다.

| 작성 항목 | 설명 |
| --- | --- |
| 화면 이름 | 시작, 캐릭터 설정, 게임 진행, 선택 결과, 인생 결과 중 하나 |
| 화면 목적 | 이 화면에서 사용자가 해야 하는 행동 |
| 표시 요소 | 텍스트, 이미지, 지표, 결과 등 화면에 보여줄 정보 |
| 버튼·입력 | 사용자가 누르거나 입력하는 요소 |
| 다음 이동 화면 | 버튼을 누른 뒤 이동할 화면 |
| 필요한 데이터 | 사건 ID, 이미지 ID, 선택지, 지표 등 화면에 필요한 데이터 |

완료 기준은 화면 5개가 모두 작성되고, 각 화면에 버튼·입력과 다음 이동 화면이 빠지지 않는 것이다.

### `docs/ui_ux_guideline.md`

UI/UX 디자인 담당이 작성한다. 화면 5개마다 최소 3개 이상의 디자인 기준을 적는다.

반드시 포함할 기준은 다음과 같다.

- 상황 이미지와 텍스트의 배치 기준
- 선택 버튼의 최소 크기와 줄바꿈 기준
- 긴 상황 문장의 스크롤 기준
- 7개 지표를 한 화면에 보여줄 방식
- 돌발 이벤트 화면을 일반 사건과 구분하는 시각적 방식
- 아기 사진과 상황 이미지가 함께 보일 때의 우선순위
- 작은 화면에서 글씨가 잘리지 않게 하는 기준

완료 기준은 화면별 기준이 분리되어 있고, 실제 Android 화면을 만들 때 바로 참고할 수 있을 정도로 구체적인 것이다.

### `docs/game_rules.md`

백엔드·게임 로직 담당이 작성한다. 아래 규칙을 반드시 항목별로 정리한다.

- 7개 지표: 건강, 운동능력, 지력, 사회성, 경제력, 행복, 운
- 지표 범위: 0~100
- 일반 선택의 지표 변화량 권장 범위: 예를 들어 -10~+10
- 돌발 이벤트 등장 확률 초안: 예를 들어 단계별 5~10%
- 사망 위기와 실제 사망의 차이
- 결말 조건이 여러 개 충족될 때의 우선순위
- 같은 선택이 두 번 눌렸을 때 한 번만 반영하는 규칙
- 조건에 맞는 사건이 없을 때 공통 사건으로 넘어가는 규칙

예시 계산을 최소 5개 넣는다.

| 예시 | 작성 내용 |
| --- | --- |
| 일반 선택 계산 | 지력 50에서 지력 +6 선택 시 지력 56 |
| 경계값 계산 | 건강 98에서 건강 +5 선택 시 건강 100 |
| 위험 선택 계산 | 건강 3에서 건강 -8 선택 시 건강 0, 사망 위기 조건 확인 |
| 돌발 이벤트 계산 | 운 70 이상이면 사망 위기가 부상으로 완화되는 조건 |
| 결말 조건 계산 | 사회성 75, 행복 60, 관련 경험 보유 시 공동체형 결말 후보 |

분기 조건 예시도 최소 20개 작성한다. 예를 들어 `지력 70 이상`, `운동능력 60 이상`, `reading_interest 보유`, `건강 20 이하`, `운 80 이상`처럼 사건 등장 조건에 쓸 수 있는 형태로 작성한다.

완료 기준은 규칙만 적는 것이 아니라, 실제 계산 예시 5개, 분기 조건 예시 20개, 예외 상황 처리를 포함하는 것이다.

### `data/events.sample.json`

시나리오·콘텐츠 작성 담당이 작성한다. 사건은 최소 80개 작성한다.

- 유아기 15개 이상
- 아동기 20개 이상
- 청소년기 20개 이상
- 성인기 20개 이상
- 특수 돌발 이벤트 5개 이상

각 사건은 다음 JSON 항목을 포함한다.

```json
{
  "eventId": "child_001",
  "stage": "아동기",
  "type": "normal",
  "title": "방과 후 선택",
  "text": "학교가 끝났다. 오늘 남은 시간을 어떻게 보낼까?",
  "imageId": "img_child_school_001",
  "conditions": {
    "requiredStats": {},
    "requiredFlags": [],
    "blockedFlags": []
  },
  "choices": [
    {
      "choiceId": "A",
      "label": "도서관에서 책을 읽는다.",
      "statDelta": {
        "지력": 6,
        "행복": 1
      },
      "resultText": "새로운 지식을 얻고 독서에 관심이 생긴다.",
      "addFlags": ["reading_interest"]
    },
    {
      "choiceId": "B",
      "label": "친구들과 운동장에서 논다.",
      "statDelta": {
        "운동능력": 4,
        "사회성": 4,
        "행복": 2
      },
      "resultText": "친구들과 가까워지고 몸을 움직이며 기분이 좋아진다.",
      "addFlags": ["playground_friendship"]
    }
  ]
}
```

작성 규칙은 다음과 같다.

- 모든 사건에는 `eventId`, `stage`, `type`, `title`, `text`, `imageId`, `conditions`, `choices`를 넣는다.
- 모든 사건에는 A·B 선택지 2개를 넣는다.
- 각 선택지에는 `choiceId`, `label`, `statDelta`, `resultText`, `addFlags`를 넣는다.
- `statDelta`에는 7개 지표 중 변화가 있는 지표만 적는다.
- 특수 돌발 이벤트는 `type`을 `special`로 적는다.
- 문장은 실제 게임에 바로 넣을 수 있게 완성된 문장으로 작성한다.

완료 기준은 JSON 형식이 깨지지 않고, 80개 사건 모두 A·B 선택지와 결과 문장이 있는 것이다. 사건 80개를 모두 완성 문장으로 쓰기 어렵다면, 최소 40개는 완성 문장으로 작성하고 나머지 40개는 제목, 성장 단계, 선택지 초안, 의도한 지표 변화까지 작성한다. 단, 빈 사건은 허용하지 않는다.

### `data/endings.sample.json`

DB·콘텐츠 데이터 담당이 작성한다. 결말은 최소 20개 작성한다.

각 결말은 다음 JSON 항목을 포함한다.

```json
{
  "endingId": "ending_balanced_life",
  "title": "균형을 이룬 삶",
  "priority": 50,
  "conditions": {
    "minStats": {
      "건강": 45,
      "운동능력": 45,
      "지력": 45,
      "사회성": 45,
      "경제력": 45,
      "행복": 60
    },
    "requiredFlags": []
  },
  "summary": "크게 튀지는 않았지만 여러 선택을 균형 있게 쌓아 안정적인 삶을 만들었다."
}
```

완료 기준은 결말 20개 모두 `endingId`, `title`, `priority`, `conditions`, `summary`를 가지고 있고, 적어도 10개 이상은 서로 다른 지표 조합으로 도달하도록 조건이 나뉘어 있는 것이다. 최종 목표인 결말 30개 이상으로 확장할 수 있도록 결말 ID 규칙도 함께 적는다.

### `data/content_schema.md`

DB·콘텐츠 데이터 담당이 작성한다. 다음 항목의 의미를 표로 정리한다.

- `eventId`
- `stage`
- `type`
- `title`
- `text`
- `imageId`
- `conditions.requiredStats`
- `conditions.requiredFlags`
- `conditions.blockedFlags`
- `choices.choiceId`
- `choices.label`
- `choices.statDelta`
- `choices.resultText`
- `choices.addFlags`
- `endingId`
- `priority`
- `summary`
- 저장 상태 항목: `runId`, `currentEventId`, `stats`, `flags`, `history`, `selectedBabyImageId`
- 이미지 항목: `imageId`, `usage`, `description`, `fileName`

각 항목은 다음 표 형식으로 작성한다.

| 항목 | 자료형 | 필수 여부 | 설명 | 예시 |
| --- | --- | --- | --- | --- |
| eventId | string | 필수 | 사건을 구분하는 고유 ID | child_001 |

추가로 `data/image_ids.sample.json`에 이미지 ID를 최소 80개 작성한다. 실제 이미지 파일이 없어도 된다. 대신 어떤 사건에 연결할 이미지인지 설명을 적는다.

완료 기준은 사건, 선택지, 결말, 이미지, 저장 상태에 필요한 항목이 빠지지 않고, 이미지 ID 80개 이상과 결말 20개 이상이 작성되는 것이다.

### `docs/test_cases.md`

팀장: 총괄·기획, QA·문서화 담당이 작성한다. 테스트는 일반, 위험, 애매한 사례로 나눈다. 각 묶음은 최소 10개 이상 작성한다.

| 묶음 | 테스트 예시 |
| --- | --- |
| 일반 사례 | 선택 A를 누르면 지표가 한 번만 증가한다. |
| 일반 사례 | 조건을 만족하면 조건부 사건이 등장한다. |
| 위험 사례 | 같은 선택 버튼을 빠르게 두 번 눌러도 지표가 두 번 반영되지 않는다. |
| 위험 사례 | 건강이 0 미만으로 내려가려 할 때 0으로 제한된다. |
| 위험 사례 | 결말 조건을 여러 개 만족할 때 우선순위가 높은 결말이 선택된다. |
| 애매한 사례 | 돌발 이벤트에서 사망 위기와 실제 사망을 어떻게 구분할지 확인한다. |
| 애매한 사례 | 선택지 결과가 좋은지 나쁜지 명확하지 않은 경우 지표 변화를 어떻게 줄지 확인한다. |
| 애매한 사례 | 이미지가 없는 사건은 기본 이미지를 쓸지 사건을 제외할지 결정한다. |

각 테스트는 다음 항목을 포함한다.

| 항목 | 설명 |
| --- | --- |
| 테스트 ID | TC-W3-001 형식 |
| 구분 | 일반 / 위험 / 애매 |
| 확인 대상 파일 | 어떤 숙제 파일을 확인하는지 |
| 조건·입력 | 어떤 상황을 넣고 확인하는지 |
| 기대 결과 | 통과 기준 |

완료 기준은 일반 10개, 위험 10개, 애매 10개 이상이 작성되고, 각 테스트에 기대 결과가 있는 것이다.

### `docs/8week_scope.md`

팀장: 총괄·기획, QA·문서화 담당이 작성한다. 남은 8주 안에 완성할 범위를 정리한다.

| 항목 | 이번 주 목표 | 8주 안 목표 | 최종 확장 목표 |
| --- | --- | --- | --- |
| 사건 | 80개 이상 | 200개 이상 | 300~500개 이상 |
| 선택지 | 160개 이상 | 400개 이상 | 1,000개 이상 |
| 결말 | 20개 이상 | 30개 이상 | 30개 이상 |
| 돌발 이벤트 | 5개 이상 | 30개 이상 | 50개 이상 |

완료 기준은 현실적으로 구현할 MVP 범위와 사용자에게 볼륨 있어 보이는 확장 범위를 나누고, 어떤 항목을 8주 안에 반드시 끝낼지 표시하는 것이다.

### `docs/before_after_check.md`

변경 전후 비교 기준을 작성한다.

| 비교 항목 | 변경 전 | 변경 후 확인 기준 |
| --- | --- | --- |
| 사건 데이터 | 사건 ID와 선택지 구조가 없음 | 이번 주 사건 80개 이상에 ID, 단계, 이미지 ID, A·B 선택지가 있음 |
| 지표 계산 | 문서에 규칙만 있음 | 각 선택지에 7개 지표 중 필요한 변화량이 들어감 |
| 결말 | 예시 결말만 있음 | 최소 20개 결말과 조건, 우선순위가 있음 |
| UI 흐름 | 화면 이름만 있음 | 화면별 표시 요소와 사용자 동작이 있음 |
| 테스트 | 검증 기준만 있음 | 일반·위험·애매한 사례가 각 10개 이상 나뉘어 있음 |

## 5. 제출 방식

각 담당자는 이번 주 안에 자신이 맡은 파일을 Markdown 또는 JSON 형식으로 작성한다. 아직 실제 Android 코드가 없으므로, 코드 파일 수정 대신 다음 구현에 바로 옮길 수 있는 데이터와 문서 형태로 제출한다.

팀장은 모든 파일을 모아 다음 항목을 확인한다.

- 파일명과 형식이 맞는가
- 초안의 프로젝트 목표와 충돌하지 않는가
- 7개 지표 이름을 통일했는가
- 사건, 선택지, 결말 데이터가 앱 구현에 사용할 수 있을 정도로 구체적인가
- 일반, 위험, 애매한 테스트가 모두 있는가

## 6. 커밋 기준

요청이 있으면 숙제 파일을 모은 뒤 Git 커밋까지 진행한다. 커밋 전에는 변경 파일 목록, 생성 파일 목록, 문서 형식 오류 여부를 확인한다.
